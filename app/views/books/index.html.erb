<table>
  <thead>
    <tr>
      <td>Title</td>
      <td>Author</td>
      <td><!-- More info --></td>
      <td><!-- Download --></td>
    </tr>
  </thead>
  <tbody>
    <% @books.each do |book| %>
      <tr>
        <td><%= book.title %></td>
        <td><%= book.author.name %></td>
        <td><%= button_to "More info", book, method: :get %>
        <td><%= button_to "Download", download_path(id: book.id), method: :get %></td>
      </tr>
    <% end %>
  </tbody>
</table>
